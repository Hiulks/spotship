DATABASE_URL="file:./dev.db"
JWT_SECRET="change-me-in-production"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Stripe (optional — demo mode without keys)
STRIPE_SECRET_KEY=""
STRIPE_WEBHOOK_SECRET=""
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=""

# Email via SMTP (optional — logs to data/outbox/ without SMTP)
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="noreply@yourdomain.com"
SMTP_SECURE="false"

# SMS via Twilio (optional — simulated without keys)
TWILIO_ACCOUNT_SID=""
TWILIO_AUTH_TOKEN=""
TWILIO_FROM=""
